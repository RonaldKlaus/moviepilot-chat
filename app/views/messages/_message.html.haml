.card
  .card-block
    .row
      .col-md-1
        = gravatar_for message.user
      .col-md-2
        .card-text
          %span.text-muted= "#{message.user.username} #{message.created_at.strftime('%H:%M:%S %d %B %Y')}"
          = message.body
